#include <tunables/global>

profile /usr/sbin/nginx flags=(complain) {
  #include <abstractions/base>
  
  capability net_bind_service,
  network inet tcp,
  
  /usr/sbin/nginx mr,
  /var/log/nginx/*.log w,
  /usr/share/nginx/html/** r,
  
  deny /etc/shadow r,
}